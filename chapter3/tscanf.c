#include<stdio.h>

int main(void)
{
    int i, j;
    // float x, y;
    // scanf("%d%d%f%f", &i, &j, &x, &y);
    // printf("i: %d\n", i);
    // printf("j: %d\n", j);
    // printf("x: %.2f\n", x);
    // printf("y: %.2f\n", y);
    scanf("%d%d", &i, &j);
    printf("i: %d, j: %d\n", i, j);
    return 0;
}